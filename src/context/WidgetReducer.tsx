
import { InitialWidgetState } from "./ConfigureWidgetState";
import { Action } from "./../global/global";

export default (state : InitialWidgetState, action: Action) => {
    switch (action.type) {
      default:
        return state;
    }
  };